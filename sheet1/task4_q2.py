text = "Hello world."
print(text)

team = [ "Chung", "Vignesh", "Baki", "Vn"]
teamSize = len(team)
print(team[0] + ". ." + team[teamSize - 1]) # as python list is 0 based so if we run the code team[teamSize], it will exceed the memory allocated to the array